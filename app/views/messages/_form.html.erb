<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: false, required: true, placeholder: "Écris ta réponse" %>
  <%= f.button :submit, "Répondre", class: "btn btn-primary" %>
<% end %>
